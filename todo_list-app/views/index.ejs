<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ToDoリスト</title>
</head>
<body>
    <h1>ToDoリスト</h1>

    <!-- 未完了タスクのセクション -->
    <h2>未完了のToDo</h2>
    <ul>
        <% todos.forEach((todo, index) => { %>
            <% if (!todo.completed) { %>
                <li>
                    <%= todo.title %> - <%= todo.description %> (期限: <%= todo.dueDate %>)
                    <a href="/todos/edit/<%= index %>">編集</a> |
                    <a href="/todos/delete/<%= index %>">削除</a> |
                    <a href="/todos/add_complete/<%= index %>">完了</a>
                </li>
            <% } %>
        <% }); %>
    </ul>
    <a href="/todos/add">新しいToDoを追加</a>
    <a href="/todos/complete_todos">完了したタスクを確認</a>
</body>
</html>
